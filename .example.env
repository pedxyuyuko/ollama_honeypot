# Ollama Honeypot Configuration
# This file contains all configurable environment variables for the honeypot.
# Copy this file to .env and modify the values as needed.

# Enable debug mode (set to 1 for debug logging, default: 0)
DEBUG=0

# Port to bind the server to (default: 11434, Ollama's default port)
PORT=11434

# Path to the directory containing mock data files (default: ./mock)
MOCK_PATH=./mock

# Path to the audit log file (optional, if not set, logs only to console)
LOG_PATH=./logs

# Download speed in bytes per second for simulating model pulls (default: 52428800, which is 50MB/s)
DOWNLOAD_SPEED=52428800

# Variance factor for random speed fluctuations (0.0 to 1.0, default: 0.2 for 20% variance)
DOWNLOAD_SPEED_VARIANCE=0.2

# Period in seconds for sinusoidal speed variation (default: 5.0)
DOWNLOAD_SPEED_WAVE_PERIOD=1.0

# Amplitude factor for sinusoidal speed variation (0.0 to 1.0, default: 0.5 for 50% amplitude)
DOWNLOAD_SPEED_WAVE_AMPLITUDE=0.5
