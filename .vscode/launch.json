{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "args": [
                "serve"
            ],
            "program": "${workspaceFolder}"
        }
    ]
}